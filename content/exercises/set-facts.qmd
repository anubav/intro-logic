Prove the following claims:

(a). If $X\subset Y$, then $X\cup Z\subset Y\cup Z$.

(b). $X\subset Y$ iff $X = X\inter Y$

(c). $X\subset Y^c$ iff $Y\subset X^c$

(d). $X\cup(Y-X) = X\cup Y$

(e). $X\inter(Y\cup Z) = (X\inter Y)\cup(X\inter Z)$

(f). $X\inter Y  = (X^c\cup Y^c)^c$
